//#region Imports

import { makeStyles } from '@material-ui/core/styles';

//#endregion

const useStyles = makeStyles({
    text: {
        fontSize: 14
    },
    selected: {
        color: '#A9A9A9'
    }
});

export default useStyles;
