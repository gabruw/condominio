//#region Imports

import { makeStyles } from '@material-ui/core/styles';

//#endregion

const useStyles = makeStyles({
    buttonContainer: {
        marginTop: 30
    }
});

export default useStyles;
